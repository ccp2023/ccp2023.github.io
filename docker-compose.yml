services:
  jekyll:
    build: .
    volumes:
      - .:/site
    command: sh -c 'cd /site; bundle install; bundle exec jekyll serve --watch --host 0.0.0.0 --incremental'
    ports:
      - 4000:4000
